security:
    authentication:
        cookieName: clientId 
        #cookieDomain: 'example.com'   # (string|domain)
        cookieSamesite: Strict          # (Strict|Lax|None) defaults to Lax

constants:
    TABLE: 'client'